# 来源说明

本文件夹集中存放定稿论文会使用的结果表，按类别分目录。原始文件路径如下，复制后未改动内容。

## 01_正文主表

- `rq1_chronic_att.csv`：`rq_final_v2/rq1_chronic_version/rq1_chronic_att.csv`
- `rq1_chronic_cohort_att.csv`：`rq_final_v2/rq1_chronic_version/rq1_chronic_cohort_att.csv`
- `rq2_three_group_ctrl8_v2.csv`：`rq_final_v2/rq2_three_group_ctrl8_v2.csv`
- `rq2_three_group_ctrl8_contrasts_v2.csv`：`rq_final_v2/rq2_three_group_ctrl8_contrasts_v2.csv`
- `rq2_three_group_pre_trend_v2.csv`：`rq_final_v2/rq2_three_group_pre_trend_v2.csv`

## 02_稳健性

- 均来自 `rq_final_v2/` 根目录或 `rq_final_v2/supplementary/`，文件名与源文件同名。

## 03_附录

- 均来自 `rq_final_v2/` 根目录，文件名与源文件同名。

## 04_诊断

- 四个 `rq2_diag_*.csv` 来自 `rq2_final_v1/diagnostics/`；
- `sample_flow.csv` 来自 `rq1_rq2_final_v2/sample_flow.csv`。

注意：诊断表来自旧目录，投稿前需核对是否基于“慢病主样本 + 堆叠 DID + 8 控制”的新口径，不一致要重跑。

## 05_RQ3

- 全部来自当前工作区 `outputs/RQ3_stage1_recommended/`，由 `RQ3_stage1_recommended.R` 与 `RQ3_count_trend.R` 生成。

## 06_数据

- `data_stacked_v2.csv`：`rq_final_v2/data_stacked_v2.csv`（统一分析数据，未改动）。

## 待办

1. 确认诊断表口径；
2. 事件研究图用堆叠口径重新出图；
3. 构成对比表补每组合基线人数与城市数。
