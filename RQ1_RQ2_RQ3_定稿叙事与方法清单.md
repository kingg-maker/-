# RQ1-RQ3 定稿叙事、计量方程与审稿人评价

口径基线：慢病主样本（`chronic_base==1`）、2011-2018、堆叠 DID（cohort×unit + cohort×wave）、never-treated 对照、地级市聚类、8 控制变量在组均值处计算 ATT。

## 一、总叙事线

1. RQ1：宽带中国是否改善慢病老年人健康能力？答案是“是，但维度分化”：功能健康与三维综合指数显著改善，认知、心理、自评不显著。
2. RQ2：收益是否流向数字弱势群体？答案是“是，但非线性”：收益集中在基期高数字脆弱尾部（0.8333 与 1.0），High-Low 在 8 控制下显著，但线性梯度不成立。
3. RQ3：为什么集中在高脆弱尾部？答案是“不是劣势条数的简单累积，而是构成与转化条件”：动机维度驱动异质性，同总分不同构成收益显著不同，且高脆弱组上网率始终为 0，支持“转化约束”而非“上网渠道”。

## 二、RQ1：平均效应

### 1. 逻辑推理

宽带基础设施降低健康信息与医疗资源获取的摩擦，理论上通过信息赋能、医疗可及、社会资本三条渠道改善健康能力。但平均效应可能被正向与负向渠道抵消，因此应先检验平均效应，再进入异质性。

### 2. 计量方程

堆叠 DID（主估计）：

```
Y_it = β·D_it + Σ_k γ_k·(D_it × X_ik) + μ_g,i + λ_g,t + ε_it
```

- `Y_it`：func_cap、bhci_fixed、psych_cap、srh_cap_fixed、cog_cap；
- `D_it`：队列 g 的个体在 wave≥首次处理波时=1（treat_stack）；
- `μ_g,i`：队列×个体固定效应；`λ_g,t`：队列×波次固定效应；
- `X_ik`：基期 8 控制（年龄、性别、城乡、教育、婚姻、log 消费、慢性病数、城市 GDP 对数）；
- ATT = β + Σ_k γ_k·mean(X_k)，即控制交互在组均值处评估；
- 标准误按地级市聚类。

无控制版本：去掉 `Σ γ_k·(D_it×X_ik)` 一项。
队列特定 ATT：按 cohort_id=3、4 分别估计；汇总 ATT 按队列处理人数加权。

### 3. 控制变量（8 控制）

age_base、gender_base、rural_base、edu_base、marry_base、log_hhcperc（log(1+消费)）、chronic_count_base、gdp_pc_log_base。

### 4. 关键结果

- simple：func 0.0193（p=0.011）、BHCI 0.0133（p=0.003）；
- 8 控制：func 0.0187（p=0.020）、BHCI 0.0127（p=0.004）；
- common window（-2 到 0）：func 0.0198（p=0.003）、BHCI 0.0114（p=0.009）；
- 事件研究：egt=0 即显著（func p=0.008、BHCI p=0.026），预趋势 Wald 通过（p≈0.92）；
- 心理、自评、认知均不显著。

### 5. 细节与易错点

- 时间不变控制只能以“控制×处理”交互进入，主效应被个体固定效应吸收；不能把 X 当普通回归元。
- 个体固定效应是“队列×个体”，不是单纯个体；波次固定效应也是“队列×波次”。
- 事件研究预趋势的 naive Wald 只作诊断，正文应报告同时置信区间。
- 认知样本缺失率高于其他维度，结果要同时报告 N，不直接比较不同结果的样本。
- func 在扩展控制后 p≈0.05，只能写“边际稳健”，不能写“任何设定都显著”。

## 三、RQ2：数字包容效应

### 1. 逻辑推理

平均效应掩盖异质性。若数字红利被优势群体俘获，出现马太效应；若弱势群体边际收益更大，出现补偿/数字包容效应。DVI 必须基于 2011 基期，避免事后内生性。

### 2. DVI 构造

```
物质维度 dvi_mat = mean(无电脑, 无手机)          ∈ {0, 0.5, 1}
技能维度 dvi_skill = mean(低教育, 无高学历子女)    ∈ {0, 0.5, 1}
动机维度 dvi_mot = 1 - mean(无社会信息活动, 无学习活动) ∈ {0, 0.5, 1}
DVI3 = mean(dvi_mot, dvi_mat, dvi_skill)       ∈ 9 个取值
```

分组（定稿）：Low = DVI3≤0.5；Mid = 0.6667/0.75；High = 0.8333/1。

### 3. 计量方程

组内 ATT（每组分一次，与 RQ1 相同方程）：

```
Y_it = β_g·D_it + Σ_k γ_gk·(D_it × X_ik) + μ_g,i + λ_g,t + ε_it
```

组间对比（一次回归）：

```
Y_it = β·D_it + γ_Mid·(D_it×I(Mid)) + γ_High·(D_it×I(High))
        + Σ_k γ_k·(D_it×X_ik) + μ_g,i + λ_g,t + ε_it
```

High-Low = γ_High；High-Mid = γ_High − γ_Mid。

预趋势检验：

```
Y_it = Σ_e δ_e·(Egt_e × TreatedEver) + μ_g,i + λ_g,t + ε_it
```

对 e<−1 的 δ 做 Wald 联合检验。

### 4. 关键结果

- 8 控制三组 BHCI：Low 0.0027（p=0.721）、Mid 0.0128（p=0.193）、High 0.0220（p=0.0013，q=0.0038）；
- High-Low 差 0.0284（p=0.0035）；High-Mid 差 0.0167（p=0.059）；
- High 组 func 0.0244（p=0.044）；
- 三组预趋势：High p=0.457，通过；
- IPW（0.0216，p=0.006）、12 控制（0.0222，p=0.0009）、全样本（0.0195，p=0.003）均稳健；
- DVI=1 单格堆叠可估计（0.0306，p=0.006），但预趋势 p=0.0013 失败，且 `NA|1|1` 动机缺失样本放大效应，故不进主结论；
- 线性交互不稳定（堆叠 p=0.204；扩展 TWFE p=0.019），只作辅助。

### 5. 细节与易错点

- 0.25/0.75 两档是部分缺失平均（两维均值），不是真实三维档位；写论文必须说明缺失规则。
- DVI=1 的“不收敛”只出现在旧 CS-DID 版本，定稿堆叠口径下可以估计；正文不要混用两种口径。
- 组间对比必须来自同一回归的协方差，不能用两组点估计拼差。
- FDR 按结果变量跨组校正。
- “数字包容”结论写“High 组显著且 High-Low 显著”，不写“随 DVI 线性递增”。

## 四、RQ3：为什么收益集中（累积劣势还是关键约束）

### 1. 逻辑推理

两个竞争假说：累积数字劣势预测 ATT 随劣势条数单调递增；关键约束预测在特定维度或构成处跳跃。Sen 能力方法把 DVI 理解为转化约束，渠道变量（上网、就医、家庭支持）是转化条件，而不是“上网就会健康”的简单机制。

### 2. 计量方程

维度×状态核心表：在 Low/Mid/High × 维度 × 状态（0/0.5/1）单元格内重复 RQ1 方程。

三维联合模型：

```
Y_it = β0·D_it + β_mat·(D_it×dvi_mat) + β_skill·(D_it×dvi_skill)
        + β_mot·(D_it×dvi_mot) + Σ_k γ_k·(D_it×X_ik) + μ_g,i + λ_g,t + ε_it
```

同总分不同构成（在固定 DVI3 值内）：

```
Y_it = β·D_it + Σ_j δ_j·(D_it×I(pattern_j)) + Σ_k γ_k·(D_it×X_ik)
        + μ_g,i + λ_g,t + ε_it
```

计数版（条目 0-4）：

```
count_i = no_computer + no_mobile + low_edu + no_college
Y_it = β·D_it + τ·(D_it×count_i) + Σ_k γ_k·(D_it×X_ik) + μ_g,i + λ_g,t + ε_it
```

门槛检验：`D_it×I(count≥2)`、`D_it×I(count≥3)`、`D_it×I(count=4)` 的系数。

### 3. 关键结果

- 联合模型：BHCI 的 `treat×dvi_mot=0.0279`（p=0.026）显著；dvi_mat p=0.108、dvi_skill p=0.657；leave-one-out 稳定；
- 构成对比：DVI3=0.6667 时，1|0.5|0.5（动机脆弱）ATT=0.0460（p=0.002），对比 0.5|0.5|1（技能脆弱）差 0.0519，p=0.0124、q=0.0497；
- 计数：count2 0.0251（p=0.039）、count3 0.0278（p=0.002）、count4 0.0234（p=0.091）；线性趋势 p=0.431；三个门槛对比全部不显著；至少 3 项可观测时峰值位置变化；
- 机制描述：High/Mid 组基期与 2015/2018 上网率均为 0，Low 组约 3%。

### 4. 结论写法

“收益集中在高数字脆弱组，但不是劣势条数的简单累积；在同等总脆弱水平下，动机/转化维度不同的群体收益显著不同，说明转化条件比障碍数量更决定数字资源能否变成健康收益。高脆弱组上网率始终为零，进一步支持转化约束而非上网渠道。”

### 5. 细节与易错点

- 计数版要求四项全部可观测，基线剔除 2048/5299 人，必须报告；min3 稳健性显示 count3 峰值不稳定，不能写“count3 是峰值”。
- `NA|1|1`（动机缺失）在 DVI=1 内 ATT=0.1355，远高于 `1|1|1`（0.0171），说明 DVI=1 高收益部分是缺失伪影，写进局限。
- 构成对比只能保留样本足够、非缺失口径干净的组合；0.25/0.75 小格不进主表。
- 机制变量是事后变量，只能作描述性佐证，不能写成正式中介因果。

## 五、审稿人视角评价（文献标注在一旁）

1. “为什么用堆叠 DID 而不是 TWFE”：交错处理下 TWFE 有负权重与坏对照组问题（Goodman-Bacon, 2021；Callaway & Sant'Anna, 2021；Sun & Abraham, 2021）。我们采用堆叠 DID 且只用 never-treated，符合定稿口径。
2. “高脆弱组是不是本来就在不同轨迹上”：High 组预趋势 p=0.457 通过；DVI=1 单格 p=0.0013 失败，已在局限说明并只用并组结果。配套 IPW、12 控制、全样本稳健性（均为项目内结果）。
3. “收益为什么集中在弱势群体”：与 Li et al. (2025)（ADL 改善偏向老年/低教育）、Cui et al. (2025)（数字禀赋亲弱势）、Finkelstein et al. (2012)（弱势者边际收益更大）一致；与认知维度文献（Ma & Sheng, 2025；余璐, 2026）的“亲优势”差异用“功能维度亲弱势、认知维度亲优势”解释。
4. “为什么是动机维度而不是教育/技能”：联合模型 leave-one-out 稳定；构成对比在同等总 DVI 下显著。文献中动机/自我效能缺口是数字鸿沟第三层（结果与收益鸿沟）的核心（数字健康 equity 综述；Sen 能力方法下的 conversion factors）。
5. “上网率 0 是不是否定机制”：不否定，反而支持转化约束；类似地，Guo et al. (2025) 显示宽带可能通过久坐、睡眠等非上网渠道影响健康。
6. “心理健康倒 U 型/优势群体受益”：Yang et al. (2025)、Wang et al. (2026) 表明心理收益偏向年轻/高教育或特定子群，与我们的零平均效应一致。
7. “时间动态”：Wu et al. (2025) 显示 2018 扩大、2020 缩小，我们主窗口 2011-2018 可能处于前期；2020 作为稳健性（项目内已有）。
8. “测量问题”：DVI 缺失平均档、DVI=1 的 `NA|1|1`、计数缺失剔除，均需在方法/附录透明报告。
9. “多重检验”：所有表按结果变量（或结果×维度）做 BH-FDR 校正。
10. “公平性是否真的改善”：RQ2b 收入维度交互不显著，Erreygers 集中指数只作描述性，不宣称亲贫（Erreygers, 2009 方法经典；Firpo, Fortin & Lemieux, 2009 用于 RIF 分位数时）。

## 六、文献清单与来源标注

| 文献 | 用途 | 来源标注 |
| --- | --- | --- |
| Goodman-Bacon (2021) | TWFE 分解/负权重 | 方法学经典（项目文档引用） |
| Callaway & Sant'Anna (2021) | CS-DID | 方法学经典（项目文档引用） |
| Sun & Abraham (2021) | 动态处理效应 | 方法学经典（项目文档引用） |
| Athey & Wager (2019) | 因果森林 | 方法学经典（项目文档引用） |
| Sen (能力方法) | 转化约束理论 | 理论经典（RQ3 可能方向文档） |
| Erreygers (2009) | 修正集中指数 | 方法学经典（RQ2 文档） |
| Firpo, Fortin & Lemieux (2009) | RIF 分位数 | 方法学经典（第一版方案） |
| Ma & Sheng (2025) | 宽带认知效应亲优势 | 项目文献表 |
| 余璐 (2026) | 数字经济与认知衰退 | 项目文献表 |
| Li et al. (2025) | 宽带改善 ADL，亲弱势 | 项目文献表 |
| Yang et al. (2025) | 互联网与心理倒 U 型 | 项目文献表 |
| Guo et al. (2025) | 宽带负面健康渠道 | 项目文献表 |
| Wang et al. (2026) | 智慧医疗特定子群显著 | 项目文献表 |
| Wu et al. (2025) | 健康不平等先扩大后缩小 | 项目文献表 |
| Cui et al. (2025) | 数字禀赋缩小健康不平等 | 项目文献表 |
| Finkelstein et al. (2012) | 弱势者边际收益更大 | 项目文献表 |

注：项目文献表的作者与年份信息来自已有文档，正式投稿前需回到原文核对卷期页码并补全 DOI。

## 七、写作红线

1. 全文统一“慢病主样本 + 8 控制 + 堆叠 DID + 城市聚类”；
2. RQ2 主结论用 ctrl8 版数字（High-Low p=0.0035），不用旧版；
3. 不写“DVI=1 不收敛”进正文；不写“上网渠道”；不写“count3 峰值”；不写“单调梯度成立”；
4. 所有表标 FDR、N、样本流失与缺失；
5. 局限段三件事：DVI=1 预趋势失败、`NA|1|1` 缺失伪影、机制描述性。
